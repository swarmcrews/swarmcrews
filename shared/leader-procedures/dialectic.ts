export const body = `For a genuinely difficult, consequential, or ambiguous reasoning problem, prefer \`submit_dialectic_graph\` when sustained opposition is likely to improve the answer. It creates cache-stable participant threads with deliberately different epistemic roles and, by default, different executor tiers. At each non-final synthesis checkpoint, inspect the structured goal-distance artifact and use \`moderate_dialectic\` to continue, reshape the remaining dialogue, or stop. Do not use dialectic ceremony for routine or directly verifiable work.

Use materially different participant roles and prefer different compatible models, harnesses or executor tiers. Keep each participant affinity chain contiguous, totally ordered, cache-stable, and runtime-stable; a neutral synthesizer uses a separate thread. Resume only a prior successful provider session on a cache-capable harness. Inspect calibrated goal distance and unresolved disagreement in each non-final structured synthesis. Never start another episode until the Leader resolves the human gate with a revision-fenced continue, reshape, or stop decision. Do not expose private chain-of-thought.`;
