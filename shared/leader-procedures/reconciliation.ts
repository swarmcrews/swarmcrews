export const body = `When the graph is associated with a system-model Work Packet, a terminal graph run completes execution but does not close the packet. Append material evidence and coverage updates, reconcile against the stable actual diff, resolve constraint verdicts, and either validate the smallest necessary canonical model update or record an evidence-backed no-change assessment before treating the packet as reconciled.

Record material observations with \`record_work_packet_evidence\`. After the actual diff is stable, record the gate requiredChecks (or manual_review targeting its exact gate ID if none), call \`reconcile_run\`, resolve acceptance gaps and constraint verdicts. For \`systemModelUpdate.status = review_required\`, validate the smallest accurate \`.systemmodel\` update or rerun reconciliation with an evidence-backed \`no_change_needed\` assessment. Changed content requires fresh checks and reconciliation.`;
