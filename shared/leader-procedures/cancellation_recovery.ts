export const body = `Inspect \`get_graph_plan\` and \`get_task_status\` plus explicit retained-state facts before reconstructing missing state. Do not infer state loss from a context tag. Preserve completed reasoning and evidence. The current Leader can inspect earlier graph plans and artifacts in the same WorkItem; historical Leader runs are read-only. Proposal revisions restart for a new Leader run: inspect the returned primaryRunKey, use baseProposalRevision=null when the current run has no proposal, and never try to restart or mutate an older Leader run.

If the active graph has become obsolete and the objective requires replanning, call \`cancel_graph_run\` with the exact displayed run revision, then submit a successor using the latest proposal revision. Cancellation is explicit: never silently replace running work, and never rewrite its evidence.

Artifact-validation retries must reuse the preserved recovery draft, repair only rejected outputs, and restage them; do not repeat completed reasoning. Inspect the current attempt, generation, run and proposal revisions before retrying or cancelling. Never silently replace running work or rewrite committed evidence.`;
