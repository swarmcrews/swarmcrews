import type { AdapterConfig, CapabilityReport, FrozenSubmission, GradeResult, IsolationSpec, MetricDefinition, ParticipantRunSpec, RunEvent, RunHandle, ExecutionSnapshot } from "../../schemas/index.js";
export type StopReason = "cancelled" | "timeout" | "budget_exceeded" | "controller_shutdown" | "protocol_violation";
export interface StopReceipt { schemaVersion: 1; handle: RunHandle; reason: StopReason; accepted: boolean; stoppedAt: string; descendantsAccountedFor: boolean; }
export interface CollectedExecution { schemaVersion: 1; handle: RunHandle; artifacts: ReadonlyArray<{ path: string; digest: string; bytes: number }>; provenance: Record<string, unknown>; usageCoverage: "complete" | "partial" | "unavailable"; logTruncated: boolean; }
export interface ExecutionAdapter { readonly id: string; readonly version: string; preflight(config: AdapterConfig): Promise<CapabilityReport>; start(run: ParticipantRunSpec): Promise<RunHandle>; observe(handle: RunHandle, afterSequence?: number): AsyncIterable<RunEvent>; inspect(handle: RunHandle): Promise<ExecutionSnapshot>; stop(handle: RunHandle, reason: StopReason): Promise<StopReceipt>; collect(handle: RunHandle): Promise<CollectedExecution>; }
export interface IsolationBackend { readonly id: string; readonly version: string; preflight(spec: IsolationSpec): Promise<CapabilityReport>; provision(spec: IsolationSpec): Promise<{ workspaceId: string; mountPath: string }>; teardown(workspaceId: string): Promise<void>; }
export interface GraderContext { schemaVersion: 1; taskId: string; graderRevision: string; timeoutMs: number; environment: Readonly<Record<string, string>>; }
export interface Grader { readonly id: string; readonly version: string; grade(input: FrozenSubmission, context: GraderContext): Promise<GradeResult>; }
export interface ReportView { readonly id: string; readonly version: string; render(input: { results: readonly unknown[]; metrics: readonly MetricDefinition[] }): Promise<{ html: string; warnings: string[] }>; }
