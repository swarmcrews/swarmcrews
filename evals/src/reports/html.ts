import type { MetricDefinition } from "../../schemas/index.js";
import { createReportMath, type ReportRecord } from "./aggregation.js";
import { createCsvExporter } from "./export.js";
import { browserApp } from "./browser.js";
const safe = (v: unknown) => JSON.stringify(v).replaceAll('<','\\u003c').replaceAll('>','\\u003e').replaceAll('&','\\u0026').replaceAll('\u2028','\\u2028').replaceAll('\u2029','\\u2029');
export function renderOfflineReport(records: readonly ReportRecord[], definitions: readonly MetricDefinition[] = [], options: {readonly title?: string;readonly analysisVersion?: string} = {}): string {
  const title=(options.title??'Agent evaluation report').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
  // Bound free-form evidence. Callers supply public/redacted settings and summaries only.
  const redact=(value: unknown): unknown => {if(Array.isArray(value))return value.map(redact);if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value).map(([k,v])=>[k,/password|secret|credential|authorization|api.?key|access.?token/i.test(k)?'[redacted]':redact(v)]));return typeof value==='string'?value.slice(0,12000):value;};
  const bounded=records.map(r=>({...r,evidence:(r.evidence??[]).slice(0,20).map(e=>e.slice(0,2000)),task:{...r.task,prompt:r.task.prompt?.slice(0,12000)}}));
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data: blob:; connect-src 'none'"><title>${title}</title><style>body{font:16px system-ui;margin:2rem;color:#18212a;background:white}label,a{display:inline-block;margin:.5rem}table{border-collapse:collapse;width:100%;margin:1rem 0}th,td{border:1px solid #777;padding:.4rem;text-align:left;overflow-wrap:anywhere}section{overflow:auto}svg{display:block;width:100%;max-width:1000px;min-width:500px}pre{white-space:pre-wrap;overflow-wrap:anywhere}input:focus-visible,select:focus-visible,a:focus-visible,summary:focus-visible{outline:3px solid #245caa}</style></head><body><h1>${title}</h1><p>Self-contained offline report. Filters persist in the URL and local storage. Evidence is limited to 20 summaries of 2000 characters and assignments to 12000 characters; omitted logs remain in the artifact directory. Timing is milliseconds; costs are USD. All chart values have table fallbacks.</p><div id="filters" role="group" aria-label="Report filters"></div><main aria-live="polite"></main><script id="report-data" type="application/json">${safe({records:redact(bounded),definitions,analysisVersion:options.analysisVersion??'report-v2'})}</script><script>(()=>{const math=(${createReportMath.toString()})();const csv=(${createCsvExporter.toString()})(math);${browserApp}})();</script></body></html>`;
}
