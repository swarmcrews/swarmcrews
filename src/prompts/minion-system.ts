/**
 * Re-export from the canonical shared source.
 * The authoritative definition lives in shared/prompts/minion-system.ts so
 * both the server agent runtime and the client UI import the same string.
 */
export { MINION_SYSTEM_PROMPT } from "../../shared/prompts/minion-system.ts";
